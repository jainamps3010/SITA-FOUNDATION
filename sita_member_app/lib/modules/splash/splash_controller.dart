// Navigation is handled directly in SplashView via a dart:async Timer.
// This controller is kept only so the existing binding compiles.
import 'package:get/get.dart';

class SplashController extends GetxController {}
