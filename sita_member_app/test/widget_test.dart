import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test — full widget tests require service initialization
    expect(true, isTrue);
  });
}
